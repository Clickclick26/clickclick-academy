# Agent rules — ClickClick Academy

Before work: read **`CLAUDE.md`**, then **`README.md`**.

## Hard rules

- Separate from marketing site (`clickclick-landing`). Never merge into that root.
- Pack codes gate courses via `packs.json` → `courseIds`. Hidden courses must not appear.
- Static only: `index.html`, `app.js`, `styles.css`, `courses.json`, `packs.json`.
- Deploy = push `main` (GitHub Pages).

## Live

- https://clickclick26.github.io/clickclick-academy/
- Target domain: `academy.clickclick.video` (DNS CNAME on 123-reg)
